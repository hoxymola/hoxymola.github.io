---
title: >-
  [백준/코틀린] 9935번: 문자열 폭발
difficulty: 골드 4
categories: [백준, CLASS 4]
tags: [Kotlin, 자료 구조, 문자열, 스택]
description: 스택에 문자를 쌓으며 폭발 문자열이 완성되면 즉시 제거해 최종 문자열을 구하는 Kotlin 스택 문제입니다.
---

## 링크
[9935번: 문자열 폭발](https://www.acmicpc.net/problem/9935)

## 풀이
`StringBuilder`를 <span class="txt_bg">스택처럼 활용</span>하여 문자를 하나씩 처리합니다.

스택의 마지막 부분이 폭발 문자열과 같아지는 순간,\
해당 길이만큼 스택에서 제거합니다.\
이후에도 스택에는 아직 처리되지 않은 문자열만 남아 있으므로,\
새롭게 이어진 부분에서 발생하는 폭발도 자연스럽게 처리됩니다.

## 코드
```kotlin
fun main() {
    val a = readln()
    val b = readln()
    val stack = StringBuilder()

    a.forEach {
        stack.append(it)
        if (stack.length >= b.length &&
            stack.substring(stack.length - b.length, stack.length) == b
        ) {
            stack.setLength(stack.length - b.length)
        }
    }

    println(
        if (stack.isEmpty()) "FRULA"
        else stack.toString()
    )
}

```
