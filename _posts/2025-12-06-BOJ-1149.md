---
title: >-
  [백준/코틀린] 1149번: RGB거리
difficulty: 실버 1
categories: [백준, CLASS 4]
tags: [Kotlin, 다이나믹 프로그래밍]
description: 인접한 집의 색이 다르도록 RGB로 칠할 때의 최소 비용을 구하는 Kotlin DP 문제입니다.
---

## 링크
[1149번: RGB거리](https://www.acmicpc.net/problem/1149)

## 풀이
`dp[i][C]`: `i`번째 집을 색 `C`로 칠했을 때의 최소 누적 비용

`i`번째 집을 색 `C`로 칠하려면,\
`i-1`번째 집은 반드시 `C`가 아닌 두 색 중 하나여야 합니다.

따라서 `dp[i][C]`는\
입력으로 받은 `i`번째 집의 색칠 비용에\
`dp[i−1][~C]` 중 최솟값을 더한 값으로 계산됩니다.

## 코드
```kotlin
const val R = 0
const val G = 1
const val B = 2 

fun main() {
    val n = readln().toInt()
    val dp = List(n + 1) { MutableList(3) { 0 } }

    for (i in 1..n) {
        val (r, g, b) = readln().split(" ").map { it.toInt() }

        dp[i][R] = r + minOf(dp[i - 1][G], dp[i - 1][B])
        dp[i][G] = g + minOf(dp[i - 1][R], dp[i - 1][B])
        dp[i][B] = b + minOf(dp[i - 1][R], dp[i - 1][G])
    }

    println(dp[n].minOrNull())
}

```
